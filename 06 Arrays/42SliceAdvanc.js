const months=['Jan','march', 'April', 'June', 'July'];
const indexOfMonth = maths.indexOf('march')

if (indexOfMath != -1)   //-1 not find a vlaue
{
    const updateMonth = months.splice(indexOfMonth,'March');
    console.log(months);
}
else{
    console.log('No such data found');
}